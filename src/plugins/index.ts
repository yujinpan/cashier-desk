import {
  Button,
  Form,
  FormItem,
  Input,
  InputNumber,
  RadioButton,
  Table,
  TableColumn,
} from 'element-ui';
import Vue from 'vue';
import './echarts';

Vue.use(Button);
Vue.use(Form);
Vue.use(FormItem);
Vue.use(Table);
Vue.use(TableColumn);
Vue.use(Input);
Vue.use(InputNumber);
Vue.use(RadioButton);
